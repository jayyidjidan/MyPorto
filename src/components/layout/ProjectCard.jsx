import Button from "../common/Button";
import Chip from "../common/Button";

export default function ProjectCard({
    href,
    varian = "middle",
    children,
    className = "",
    ...props
}){
  const base = "flex "
}